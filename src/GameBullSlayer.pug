script.
    export let bulls = 5

div
    ul.board
      {#each [...Array(bulls).keys()] as row}
        li Bull \#{row+1}.
          input("type"="checkbox")

style.
    input[type="checkbox"] {
        width: 20px
    }

    ul.board {
        list-style-type: none;
        padding: 0; 
        margin: 0; 
        display: flex;
        flex-wrap: wrap;
    }

    ul.board li {
        height: 40px;
        flex: 1 50%;
    }
